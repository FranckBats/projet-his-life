{% extends 'base.html.twig' %}

{% block title %}Hello NoteController!{% endblock %}

{% block body %}
<div class="row">
    <h1>Mots des professeurs</h1>
</div>



<div>
	<a href="{{path('note_add')}}" class="btn btn-primary">Ajouter un message du professeur</a>
	<a href="{{path('school')}}" class="btn btn-primary">Retour</a>
</div>

{% for note in notes %}
    <div class="card col-3">
    <div class="card-body">
        <h5 class="card-title">{{ note.name }}</h5>
        <img src="{{asset(note.file)}}" alt="">
        {# <a href="{{ path('movie_read_slug', {'slug': movie.slug}) }}" class="card-link">Voir la fiche</a> #}
    </div>
    
   
{% endfor %}
    <div class="card-btn">
						<a href="#" class="btn btn-primary">Modifier</a>
						<a href="#" class="btn btn-primary">Supprimer</a>
	 			</div>
    </div>	 
</div>
{% endblock %}