<div class="row">
    <h1>Entrée du carnet de santé</h1>
</div>

<div class="row">


    <div class="card col-3">
    <div class="card-body">
        <h5 class="card-title">{{ healthbook.name }}</h5>
        <img src="{{asset(healthbook.file)}}" alt="">
        {# <a href="{{ path('movie_read_slug', {'slug': movie.slug}) }}" class="card-link">Voir la fiche</a> #}
    </div>
    </div>


</div>