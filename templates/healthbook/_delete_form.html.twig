<form method="post" action="{{ path('healthbook_delete', {'id': healthbook.id}) }}" onsubmit="return confirm('Etes vous sûre de vouloir supprimer ce fichier ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ healthbook.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>