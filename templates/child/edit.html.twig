{% extends 'base.html.twig' %}

{% block title %}Modifier le profil enfant{% endblock %}

{% block body %}



<div class="col-4 mt-2">

{{ form_start(form) }}
	{{ form_row(form.lastname) }}
	{{ form_row(form.firstname) }}
	{{ form_row(form.gender) }}
	{{ form_row(form.birthdate) }}
	{{ form_row(form.picture) }}
	{{ form_row(form.families)}}



<a href="{{path('child_read', {'id': child.id})}}" class="btn btn-primary "><button type="submit" class="btn btn-primary">Modifier</button></a>

{{ form_end(form) }}

    

{% block javascripts %}{% endblock %}
{% endblock %}