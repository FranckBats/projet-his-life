{% extends 'basedashboard.html.twig' %}

{% block title %}His Life
{% endblock %}

{% block stylesheets %}
	
	
	

{% endblock %}
{% block body %}
  <main>
	<h1>
		Ajouter un Enfant
	</h1>

	{{ form_start(form) }}
	{{ form_row(form.lastname) }}
	{{ form_row(form.firstname) }}
	{{ form_row(form.gender) }}
	{{ form_row(form.birthdate) }}
	{{ form_row(form.picture) }}
	{{ form_row(form.families)}}
	{{ form_end(form) }}
	
	<div class="btn-ajout">
		<a href="{{path ('child_profile') }}" class="btn btn-primary">Ajouter un Enfant</a>
	</div>

	</main>
	{% block javascripts %}{% endblock %}

{% endblock %}
